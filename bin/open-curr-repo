#!/bin/zsh

if git rev-parse --is-inside-work-tree &>/dev/null; then
    xdg-open "$(git remote get-url origin | sed 's/git@github.com:/https:\/\/github.com\//')"  
else
    echo "Not currently inside a Git repository."
fi
